name: Windows

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: Generate
        run: scripts/gen.bat -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: scripts/build.bat
